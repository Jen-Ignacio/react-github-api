import React from 'react';

export default function MainHeader(){
    return(
        <header id='main-header' className="hero">
            <h1 className="title hero-body">GITHUB API CALL LIST</h1>
        </header>
    )
}